# Copy to env/secrets.env and fill in. The real file is gitignored.

# Salesforce Connected App's Consumer Secret.
# Setup → App Manager → your Connected App → ▾ → Manage Consumer Details.
SF_CLIENT_SECRET=
